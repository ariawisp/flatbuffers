package dev.flatbuffers.cli

fun main(args: Array<String>) {
  if (args.isNotEmpty()) {
    println("flatc-kotlin CLI is not yet implemented. Received arguments: ${args.joinToString(" ")}")
  } else {
    println("flatc-kotlin CLI is not yet implemented.")
  }
}
